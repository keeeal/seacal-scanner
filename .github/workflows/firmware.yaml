name: Firmware

on:
  push:
    branches:
      - main
  pull_request:
    paths:
      - .github/workflows/firmware.yaml
      - src/firmware/**/*

jobs:
  build-firmware:
    name: Build firmware
    runs-on: ubuntu-latest
    steps:

    - name: Checkout
      uses: actions/checkout@v3
      with:
        submodules: recursive

    - name: Build docker image
      run: |
        make build service=firmware

    - name: Build binaries
      run: |
        make firmware

    - name: Archive binaries
      uses: actions/upload-artifact@v3
      with:
        name: firmware
        if-no-files-found: error
        path: |
          output/build/*.hex
